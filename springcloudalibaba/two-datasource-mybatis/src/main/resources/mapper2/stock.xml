<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tulingxueyuan.order.dao2.StockMapper">
    <select id="getStocks" resultType="com.tulingxueyuan.order.model.Stock">
        SELECT id as id,
        product_id as productId,
        count as count
        FROM stock_tbl
    </select>
    <!-- 其他 SQL 语句... -->
</mapper>